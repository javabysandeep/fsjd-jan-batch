package classComponents.variables;
public class Demo1 {
    public static void main(String[] args) {
        //variable : it is used for storing or accessing the data.
        // local, instance, static variable
        //1. requirement : to store something temporary calculations : local variable --> stack memory area
        //2. to store which is object specific : instance variable : heap memory area
        //3. to store which is common for all the objects : static variable : class or method area

        // local variable : are definded inside the method, block, constructor, method param
        // instance variable : are defined outside all components at class level without static modifier
        // static variable : are defined outside all components at class level but with static modifier.

    }
}
